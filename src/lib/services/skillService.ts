import type { ClientSideCardSkill } from "$lib/types";

// TODO: This shouldn't be ClientSideCardSkill[]
export function getSkills(skills: ClientSideCardSkill[]): ClientSideCardSkill[] {
    return skills;
}